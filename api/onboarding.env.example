# Add to private .env only after migration 005. Leave disabled until hosted testing.
CUSTOMER_ONBOARDING_ENABLED=false
# Explicit clinics.id for this deployment; do not guess or select from browser input.
CUSTOMER_CLINIC_ID=
