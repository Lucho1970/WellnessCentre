# Server-side only. Never add these values to a VITE_ variable or frontend bundle.
GOOGLE_MAPS_API_KEY=replace-with-a-server-side-restricted-google-maps-key
ADDRESS_VALIDATION_SIGNING_KEY=replace-with-at-least-32-random-characters
ADDRESS_VALIDATION_TOKEN_TTL_SECONDS=900
